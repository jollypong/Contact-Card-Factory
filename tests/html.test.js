
const { expect } = require('@jest/globals');
const { describe } = require('yargs');
const html = require('../lib/html');

describe('check html methods', () => {
    describe('check generateCard', () => {
        it ('confimrs card generated', ()=> {

            expect();
        })
    });
    describe('check generateHTML', () => {
        it ('confirms html is generated', ()=> {
            
            expect();
        })
    });
});